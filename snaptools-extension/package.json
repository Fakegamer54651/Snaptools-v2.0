{
  "name": "snaptools-extension",
  "version": "2.0.0",
  "scripts": {
    "build": "tsc && if not exist dist mkdir dist && xcopy /E /I /Y public dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}